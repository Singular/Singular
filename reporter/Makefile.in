#################################################################
###
### Makefile for Singular
###
#################################################################

SHELL		= /bin/sh

##
## various paths
##
srcdir		= .
prefix 		= @prefix@
exec_prefix 	= @exec_prefix@
libdir 		= @libdir@
# includes are taken from here
includedir	= @includedir@

##
## various programs
##

CXX		= g++
INSTALL		= @INSTALL@
INSTALL_PROGRAM	= @INSTALL_PROGRAM@
INSTALL_DATA	= @INSTALL_DATA@
MKINSTALLDIRS   = ./mkinstalldirs
LN_S		= ln -s

##
## compiler and linker options
##
PIPE		= -pipe
CFLAGS		= -O3 -w -fomit-frame-pointer ${PIPE}
CXXFLAGS	= -O3 -w -fomit-frame-pointer --no-rtti ${PIPE}
CXXTEMPLFLAGS	= -fno-implicit-templates --no-exceptions
CPPFLAGS	= -I${srcdir} -I${includedir} -I${prefix}/include/singular
DEFS		= -DNDEBUG -DOM_NDEBUG -Dix86_Linux -DHAVE_CONFIG_H

## End configuration dependend stuff
#################################################################

###
### file sets
###

# normal C++ source files
CXXSOURCES=reporter.cc

# normal C source files
CSOURCES=dError.c

SOURCES=${CSOURCES} ${CXXSOURCES}

HEADERS=reporter.h

DISTFILES=${SOURCES} ${HEADERS}

OBJS := $(CXXSOURCES:.cc=.o) $(CSOURCES:.c=.o)

.cc.o:	
	${CXX} ${CXXFLAGS} ${CXXTEMPLFLAGS} ${CPPFLAGS} ${DEFS} -c $<
.c.o:
	${CC} ${CFLAGS} ${CPPFLAGS} ${DEFS} -c $<

reporter.a: ${OBJS}
	-rm -f reporter.a
	ar cr $@ $^

all: reporter.a

install: all
	${MKINSTALLDIRS} ${includedir}
	${MKINSTALLDIRS} ${libdir}
	${INSTALL_DATA} reporter.a ${libdir}/libreporter.a
	${INSTALL_DATA} reporter.h ${includedir}/reporter.h

##
## clean targest
##
mostlyclean: 
	-rm -f *.o *.og core *.op

clean: mostlyclean
	-rm -f *.bak *.d *.dd depend *.a *.so*

tags:   
	ctags *.c *.h *.cc *.inc
